import { FormatAttackCostPipe } from './format-attack-cost.pipe';

describe('FormatAttackCostPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatAttackCostPipe();
    expect(pipe).toBeTruthy();
  });
});
